version: '3.7'

services:
  build:
    image: ghcr.io/kijimad/roam:4ba1393a
    build:
      context: .
      target: build
    volumes:
      - .:/roam
  dev:
    build:
      context: .
      target: dev
    volumes:
      - .:/roam
